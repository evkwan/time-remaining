# Public site URL (no trailing slash). Used for canonical links, sitemap, and Open Graph.
# Use the final host that the domain resolves to (apex redirects to www).
NEXT_PUBLIC_SITE_URL=https://www.yearleft.app
